"""Kitchen module — shared SQL query helpers. Filled in K2–K4."""

from __future__ import annotations

# Query constants and helpers will be added here as routes are implemented.
# Keeping this module as a dedicated home for SQL avoids cluttering the router
# and makes it easier to unit-test individual queries.
